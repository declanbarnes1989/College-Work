select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 1;

update monthly_fact 
set Total_Monthly_Income = 45240 
where month_id = 1;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 2;

update monthly_fact 
set Total_Monthly_Income = 58080
where month_id = 2;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 3;

update monthly_fact 
set Total_Monthly_Income = 27824
where month_id = 3;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 4;

update monthly_fact 
set Total_Monthly_Income = 96524
where month_id = 4;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 5;

update monthly_fact 
set Total_Monthly_Income = 13664
where month_id = 5;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 6;

update monthly_fact 
set Total_Monthly_Income = 35148
where month_id = 6;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 7;

update monthly_fact 
set Total_Monthly_Income = 34532
where month_id = 7;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 8;

update monthly_fact 
set Total_Monthly_Income = 21900
where month_id = 8;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 9;

update monthly_fact 
set Total_Monthly_Income = 18176
where month_id = 9;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 10;

update monthly_fact 
set Total_Monthly_Income = 19796
where month_id = 10;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 11;

update monthly_fact 
set Total_Monthly_Income = 42976
where month_id = 11;

select sum(total_weekly_income*4) from weekly_fact
inner join monthly_fact
on monthly_fact.Month_ID = weekly_fact.Week_ID
where Month_ID = 12;

update monthly_fact 
set Total_Monthly_Income = 121024
where month_id = 12;



































