SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 1;

update monthly_fact 
set Highest_Price_product_PM =580 
where Month_ID = 1;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 2;

update monthly_fact 
set Highest_Price_product_PM =968
where Month_ID = 2;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 3;

update monthly_fact 
set Highest_Price_product_PM =752
where Month_ID = 3;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 4;

update monthly_fact 
set Highest_Price_product_PM =1636
where Month_ID = 4;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 5;

update monthly_fact 
set Highest_Price_product_PM =244
where Month_ID = 5;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 6;

update monthly_fact 
set Highest_Price_product_PM =404
where Month_ID = 6;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 7;

update monthly_fact 
set Highest_Price_product_PM =388
where Month_ID = 7;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 8;

update monthly_fact 
set Highest_Price_product_PM =876
where Month_ID = 8;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 9;

update monthly_fact 
set Highest_Price_product_PM =284
where Month_ID = 9;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 10;

update monthly_fact 
set Highest_Price_product_PM =404
where Month_ID = 10;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 11;

update monthly_fact 
set Highest_Price_product_PM =632
where Month_ID = 11;

SELECT MAX(price*4) FROM product_dim
inner join monthly_fact
on monthly_fact.month_id = product_dim.prod_id
where month_id = 12;

update monthly_fact 
set Highest_Price_product_PM =1952
where Month_ID = 12;





































